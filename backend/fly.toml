app = 'andrevendas-api'
primary_region = 'gru' # São Paulo

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '3001'
  FRONTEND_URL = 'https://andrevalenca.pages.dev'
  PUBLIC_API_URL = 'https://andrevendas-api.fly.dev'
  CHECKOUT_RETURN_URL = 'https://andrevalenca.pages.dev/checkout/retorno'
  # Melhor Envio (frete) — sandbox até a conta definitiva do artista autorizar
  MELHORENVIO_CLIENT_ID = '27133'
  MELHORENVIO_SANDBOX = 'true'
  MELHORENVIO_USER_AGENT = 'andrevalenca (matheusachim@gmail.com)'
  MELHORENVIO_FROM_CEP = '35700353'

[http_service]
  internal_port = 3001
  force_https = true
  # máquina sempre ligada — sem cold start (shared-cpu-1x 256MB ≈ US$2/mês)
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
